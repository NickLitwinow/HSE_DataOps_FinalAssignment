apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ml-service-chart.fullname" . }}-config
  labels:
    {{- include "ml-service-chart.labels" . | nindent 4 }}
data:
  MODEL_VERSION: {{ .Values.env.modelVersion | quote }}
  LOG_LEVEL: {{ .Values.env.logLevel | default "INFO" | quote }}
